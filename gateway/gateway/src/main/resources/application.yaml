spring:
  application:
    name: gateway

    cloud:
      gateway:
        routes:
          - id: account-service
            uri: http://localhost:8081
            predicates:
              - Path=/accounts/**
          - id: auth-service
            uri: http://localhost:8082
            predicates:
              -Path=/auth/**